apiVersion: apps/v1
kind: Deployment
metadata:
  name: cicd-app-dev

spec:
  replicas: 1
  selector:
    matchLables:
      app: cicd-app-dev
  templates:
    metadata:
      labels:
        app: cicd-app-dev
